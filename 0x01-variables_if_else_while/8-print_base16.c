#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 (success)
 *
 */
int main(void)
{
	char c, b;

	for (c = 0; c <= 9; c++)
	{
		putchar(c);
	}

	for (b = 'a'; b <= 'f' b++)
	{
		putchar(b);
	}



	putchar('/n');
	return (0);

}
